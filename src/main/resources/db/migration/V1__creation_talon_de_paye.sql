CREATE TABLE talon_de_paye
(
    id                       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    date_de_paiement         date                                    NOT NULL,
    periode_date_debut       date                                    NOT NULL,
    periode_date_fin         date                                    NOT NULL,
    salaire_regulier_heures  DOUBLE PRECISION                        NOT NULL,
    salaire_regulier_montant DOUBLE PRECISION                        NOT NULL,
    salaire_regulier_cumul   DOUBLE PRECISION                        NOT NULL,
    vacances_hrs_cumul       DOUBLE PRECISION                        NOT NULL,
    vacances_cumul           DOUBLE PRECISION                        NOT NULL,
    surtemps15_hrs_cumul     DOUBLE PRECISION                        NOT NULL,
    surtemps15_cumul         DOUBLE PRECISION                        NOT NULL,
    surtemps20_hrs_cumul     DOUBLE PRECISION                        NOT NULL,
    surtemps20_cumul         DOUBLE PRECISION                        NOT NULL,
    conge_flottant_hrs_cumul DOUBLE PRECISION                        NOT NULL,
    conge_flottant_cumul     DOUBLE PRECISION                        NOT NULL,
    conge_ferie_hrs_cumul    DOUBLE PRECISION                        NOT NULL,
    conge_ferie_cumul        DOUBLE PRECISION                        NOT NULL,
    boni_hrs_cumul           DOUBLE PRECISION                        NOT NULL,
    boni_cumul               DOUBLE PRECISION                        NOT NULL,
    action_hrs_cumul         DOUBLE PRECISION                        NOT NULL,
    action_cumul             DOUBLE PRECISION                        NOT NULL,
    impot_fed_courant        DOUBLE PRECISION                        NOT NULL,
    import_fed_cumul         DOUBLE PRECISION                        NOT NULL,
    rrq_courant              DOUBLE PRECISION                        NOT NULL,
    rrq_cumul                DOUBLE PRECISION                        NOT NULL,
    assurance_emploi_courant DOUBLE PRECISION                        NOT NULL,
    assurance_emploi_cumul   DOUBLE PRECISION                        NOT NULL,
    impot_prov_courant       DOUBLE PRECISION                        NOT NULL,
    impot_prov_cumul         DOUBLE PRECISION                        NOT NULL,
    rqap_courant             DOUBLE PRECISION                        NOT NULL,
    rqap_cumul               DOUBLE PRECISION                        NOT NULL,
    CONSTRAINT pk_talon_de_paye PRIMARY KEY (id)
);